-- ─────────────────────────────────────────────────────
-- 1. bus_requests 테이블에 contracted_cost 컬럼 추가
-- ─────────────────────────────────────────────────────
ALTER TABLE bus_requests
ADD COLUMN contracted_cost INTEGER DEFAULT 0;

-- ─────────────────────────────────────────────────────
-- 2. 기존 승인 건수 처리 (Optional)
-- 기존에 '승인' 상태인 데이터가 있다면 계약 금액을 50만원으로 일괄 변경합니다.
-- ─────────────────────────────────────────────────────
UPDATE bus_requests
SET contracted_cost = 500000
WHERE status = '승인' OR status = '매칭완료' OR status = '운행완료' OR status = '정산완료';

-- ─────────────────────────────────────────────────────
-- 3. API 연동을 위해 스키마 캐시 갱신
-- ─────────────────────────────────────────────────────
NOTIFY pgrst, 'reload schema';
